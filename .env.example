# Root env example (documentation only — copy to .env where needed, never commit real secrets)
# Backend uses backend/.env; frontend uses frontend/.env (VITE_* only, no secrets).
NODE_ENV=development
