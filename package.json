{
  "name": "billiard-gamehub",
  "private": true,
  "version": "0.0.0",
  "description": "GameHub / Billiard-GameHub — project foundation",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "start:backend": "bun run --cwd backend start"
  }
}
